# Generated by scripts/generate-ca.sh:
#   - root_ca.crt
#   - intermediate_ca.crt
#   - ca.json
# These are public CA material (no private keys) and are committed so ArgoCD can render the
# step-ca ConfigMaps. Run `make generate-ca` before the first bootstrap.
