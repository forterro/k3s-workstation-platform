apiVersion: v2
name: metallb
description: MetalLB load balancer for the k3s workstation platform (seed-installed, GitOps-managed)
type: application
version: 0.1.0
dependencies:
  # renovate: registryUrl=https://metallb.github.io/metallb
  - name: metallb
    repository: https://metallb.github.io/metallb
    version: 0.16.1
