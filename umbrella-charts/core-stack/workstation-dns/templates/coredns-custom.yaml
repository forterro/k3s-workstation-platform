apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  workstation.override: |
    rewrite stop {
      name regex (.*)\.{{ .Values.domain | replace "." "\\." }} {{ .Values.traefikService }} answer auto
    }
